import styled from 'styled-components';

const Cards = styled.div`
  margin-top: 3rem;
  display: flex;
  align-items: center;

  & > * {
    margin-right: 5rem;
  }
`;

export const Styled = {
  Cards
};
